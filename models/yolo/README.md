# Orange baseline

`orange.pt` is the existing local simulation orange detector copied from Edge_AI.
Its real-camera accuracy, TensorRT speed and compressed accuracy have not been measured.
The vendor garbage-sorting `best.engine` has no orange class and is not used here.
Use `scripts/export_detector.py` to export this detector after validation/calibration.
